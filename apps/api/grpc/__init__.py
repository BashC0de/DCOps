"""Inter-site gRPC definitions.

The .proto files in this directory are compiled to Python in CI (or via
`make proto`). Generated `*_pb2.py` / `*_pb2_grpc.py` files are gitignored.

Ships: Week 9 alongside the federation work.
"""
